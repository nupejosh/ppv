			<div class="fields">
				<strong>Email address</strong>
      {{input class="form-control" type="text" value=identification placeholder="Your email"}}
			</div>
			<div class="fields">
				<strong>Password</strong>
    {{input class="form-control" type="password" value=password placeholder="Password"}}
      </div>
			<div class="actions">
				<button type="submit" class="btn btn-primary btn-lg">Sign in to your account</button>
			</div>
